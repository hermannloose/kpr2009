[namespace:pong]
	rom = rom;
	vesa = fb->open();
	PongServer;

[namespace:paddle1]
	rom = rom;
	kbd = kbd->open();
	svr = ns/pong/PongServer;

[namespace:paddle2]
	rom = rom;
	kbd = kbd->open();
	svr = ns/pong/PongServer;

rom/paddle 50 17 145 31 159
	ns = ns/paddle1, rw;
	log = logger->open("paddle1", yellow);

rom/paddle 50 30 158 32 160
	ns = ns/paddle2, rw;
	log = logger->open("paddle2", yellow);

rom/pong-server
	ns = ns/pong, rw;
	log = logger->open("pong-server", blue);

!rom/pong-client
	ns = ns/pong;
	log = logger->open("pong-client", yellow);
